package cli

// Subcommand stubs, replaced one task at a time as the port progresses.
// Each will move to its own file (install.go, add.go, ...) when implemented.

func cmdStatus(args []string) int { return notImplemented("status", "T12") }
